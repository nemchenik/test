name: Catalog Plans batch 38 master bedroom videos

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - "pinterest/batch38_master_bedroom_video.py"
      - ".github/workflows/catalog-batch38-master-bedroom-video.yml"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    steps:
      - name: Checkout generator branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Checkout public asset branch
        uses: actions/checkout@v4
        with:
          ref: catalog-plans-pinterest-assets
          path: asset_repo
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade requests beautifulsoup4 pillow
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Generate and validate 200 master-bedroom MP4 files
        env:
          ASSET_CHECKOUT: ${{ github.workspace }}/asset_repo
          ASSET_BRANCH: catalog-plans-pinterest-assets
        run: python pinterest/batch38_master_bedroom_video.py

      - name: Publish MP4 files to asset branch
        working-directory: asset_repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pinterest/generated_batch_38_master_bedroom_video
          if ! git diff --cached --quiet; then
            git commit -m "Publish Catalog Plans batch 38 master bedroom videos"
            git fetch origin catalog-plans-pinterest-assets
            git rebase origin/catalog-plans-pinterest-assets
            git push origin HEAD:catalog-plans-pinterest-assets
          fi

      - name: Verify all 200 public MP4 URLs
        env:
          ASSET_CHECKOUT: ${{ github.workspace }}/asset_repo
          ASSET_BRANCH: catalog-plans-pinterest-assets
        run: python pinterest/batch38_master_bedroom_video.py --verify-public

      - name: Commit verified CSV and validation reports to generator branch
        if: github.event_name == 'pull_request'
        env:
          HEAD_BRANCH: ${{ github.head_ref }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pinterest/batch38_video_output
          if ! git diff --cached --quiet; then
            git commit -m "Add verified Catalog Plans batch 38 Pinterest CSV [skip ci]"
            git push origin "HEAD:${HEAD_BRANCH}"
          fi

      - name: Upload verified CSV and validation reports
        uses: actions/upload-artifact@v4
        with:
          name: catalog-plans-batch38-master-bedroom-video-pins-verified
          path: |
            pinterest/batch38_video_output/catalog_plans_pinterest_master_bedroom_videos_batch_38_200.csv
            pinterest/batch38_video_output/local_validation_batch38.json
            pinterest/batch38_video_output/public_validation_batch38.json
          if-no-files-found: error
          retention-days: 14
